# Cviceni 08 — Neuron jako linearni klasifikator (aktivacni funkce, bez uceni)
# Plne zamrazene, prikvacene verze (pip freeze). Instalace: pip install -r requirements.txt
#
# Prime zavislosti: numpy (vypocty), scikit-learn (nacteni breast-cancer priznaku
# pro blok logisticke regrese), matplotlib (grafy), PyYAML (config), pytest (testy).
# Neuron i aktivacni funkce jsou psane od nuly — nic dalsiho neni potreba.
# Zbytek seznamu jsou tranzitivni zavislosti techto peti balicku.
# POZOR: cislo verzi jsou ORCHESTRATOROVYM ODHADEM (session bez nainstalovaneho
# prostredi) — po instalaci do .venv doporuceno prepsat skutecnym `pip freeze`.
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
iniconfig==2.3.0
joblib==1.5.3
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.5.2
packaging==26.3
pillow==12.3.0
pluggy==1.6.0
Pygments==2.21.0
pyparsing==3.3.2
pytest==9.1.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
scikit-learn==1.9.0
scipy==1.18.1
six==1.17.0
threadpoolctl==3.6.0
